# Групповые Telegram-отчёты WB, Ozon и Яндекс Маркета

Модуль не обращается к API маркетплейсов и не изменяет загруженные данные: читает синхронизированную PostgreSQL, формирует почасовую серию из четырёх отдельных сообщений — Wildberries, Ozon, Яндекс Маркет и общий итог — и Excel-файлы остатков всех трёх площадок, затем отправляет их в одну группу из `WB_TG_CHAT_ID`. В production текстовое расписание и Excel-задание запускаются отдельными systemd units и не делят процесс с marketplace-синхронизацией.

Telegram пишет собственные файлы в `logs/telegram/` и journal systemd; он не участвует в ротации `logs/wb/wb_sync.log` процесса WB.

Личные уведомления о выполненных и ошибочных действиях относятся к независимому [`operations_bot`](../operations_bot/README.md). Групповые отчёты не вызывают его напрямую.

## Что отправляется

- утром в `WB_TG_MORNING_TIME`: прошлый день и текущий месяц по вчера включительно, реклама, остатки, обращения и состояние синхронизации;
- каждый час (`WB_TG_OPERATIONAL_INTERVAL_SECONDS=3600`): отдельные сообщения Wildberries, Ozon, Яндекс Маркета и сводный итог; между сообщениями выдерживается `WB_TG_MESSAGE_DELAY_SECONDS`;
- каждое сообщение площадки содержит заказы, продажи, рекламу, остатки и доступный статус загрузки именно этой площадки;
- блок Ozon читает `ozon_postings`, различает уникальные заказы и отправления, показывает товары FBO/FBS, сумму и отмены; реклама читается из `ozon_ad_daily_stats`, а при отсутствии данных за сегодня явно указывается дата последнего доступного дня;
- блок Яндекс Маркета читает `yandex_market_orders`, показывает заказы и товары FBY/FBS, сумму, статусы и остатки; отсутствие подключённого источника рекламы указывается явно;
- итоговое сообщение суммирует заказы, товары, товарную сумму, отмены и доступные остатки трёх площадок; рекламный итог содержит только источники за текущий день и перечисляет непокрытые площадки;
- блок площадки выводится и при нулевом результате или ошибке последней загрузки, поэтому отсутствие данных не выглядит как отсутствие раздела;
- если последний общий WB-цикл имеет статус `partial`, блок загрузки перечисляет ошибочные задания и сохранённую причину, а не только число ошибок;
- заказы считаются по дате заказа, отмены — отдельно по дате регистрации отмены, выкупы и возвраты — по дате соответствующей операции;
- заказы FBS и FBO берутся из realtime WB Order Feed; Statistics API используется только как явно отмеченный резерв до первого успешного запуска Order Feed;
- оперативные показатели явно помечаются как предварительные, пока финансовый отчёт WB не покрывает период.

История хранится в `wb_telegram_deliveries`. Каждое из четырёх сообщений имеет собственный ключ доставки с суффиксом площадки. После частичной ошибки повторный запуск пропустит уже отправленные сообщения и продолжит с неотправленного.

## Структура модуля

- `client.py` — вызов Telegram Bot API и разбиение длинных сообщений;
- `reports.py` — расчёт и форматирование отчётов по данным PostgreSQL;
- `dispatcher.py` — резервирование доставки, отправка и запись результата;
- `stock_reports.py` — формирование WB/Ozon/Яндекс Маркет `.xlsx` полностью в оперативной памяти;
- `scheduler.py` — утреннее и интервальное расписание;
- `__main__.py` — отдельная точка запуска `python -m telegram_bot`.

## Настройка и запуск

1. Создать бота через BotFather и добавить его в группу.
2. Записать токен в `WB_TG_BOT_TOKEN`, отрицательный ID группы — в `WB_TG_CHAT_ID`.
   Задержка между сообщениями задаётся `WB_TG_MESSAGE_DELAY_SECONDS`; значение по умолчанию — 3 секунды.
3. Выполнить `python -m alembic upgrade head` и запустить timers загрузки заказов WB, Ozon и Яндекс Маркета.
4. Дождаться успешных запусков `python -m wb.order_feed_sync`, `python -m ozon --task orders` и `python -m yandex_market --task orders`.
5. После обновления кода обязательно перезапустить долгоживущий процесс: `sudo systemctl restart wbozon-telegram.service`.
6. Проверить все три блока принудительным отчётом: `python -m telegram_bot --once operational --force`.

Если в сообщении остался только прежний блок WB, сначала сравните установленную версию и перезапустите сервис:

```bash
git rev-parse --short HEAD
sudo systemctl restart wbozon-telegram.service
sudo journalctl -u wbozon-telegram.service -n 50 --no-pager
./.venv/bin/python -m telegram_bot --once operational --force
```

`--force` нужен для проверки внутри уже отправленного часового окна: плановая доставка защищена от повторов ключом `operational:<номер окна>`.

Ручной запуск расписания отчётов: `python -m telegram_bot`. WB запускается независимо командой `python -m wb`.

Разрешённые пользователи не используются: модуль не принимает команды и отправляет сообщения только в настроенный `chat_id`. Для принудительной ручной повторной отправки есть `--force`.

Для просмотра доступных режимов: `python -m telegram_bot --help`.

## Excel-файлы остатков через systemd timer

Команда читает ежедневные срезы из PostgreSQL, создаёт три Excel-файла полностью в памяти и отправляет их как документы. На файловую систему сервера отчёты не записываются.

- дата отчёта — текущая дата в `Europe/Moscow`; при запуске в 09:00 используется срез на 00:00 этого же дня;
- `wb_stocks_YYYY-MM-DD.xlsx` содержит листы FBS и FBO;
- `ozon_stocks_YYYY-MM-DD.xlsx` содержит листы «По складам» и «Агрегат»;
- `yandex_market_stocks_YYYY-MM-DD.xlsx` содержит листы «Сводка» и «По складам»;
- складской лист Ozon включает ID, понятное название склада и кластер;
- строки с `quantity = 0` для WB, `present = 0` для Ozon и `count = 0` для Яндекс Маркета в Excel не включаются;
- файлы создаются через `BytesIO`, после отправки удалять на сервере нечего.

```bash
python -m telegram_bot --once stock-files
```

Для повторной отправки конкретной даты:

```bash
python -m telegram_bot --once stock-files --date 2026-08-19 --force
```

Production timer `wbozon-telegram-stock.timer` запускает команду ежедневно в 09:00 по Москве:

```bash
sudo systemctl enable --now wbozon-telegram-stock.timer
systemctl list-timers wbozon-telegram-stock.timer --all
```

Старую cron-строку после включения timer нужно удалить, чтобы оставался один механизм расписания.

WB, Ozon и Яндекс Маркет имеют независимые ключи доставки. Если отправка одного файла завершится ошибкой, следующий запуск не продублирует уже успешно отправленный файл.

Если срез WB, Ozon или Яндекс Маркета за текущую московскую дату отсутствует, бот отправляет в группу предупреждение с датой и названием отсутствующего источника. Предупреждение также защищено от повторной отправки за одну дату; файл по доступному источнику отправляется независимо.

## Диагностические уведомления

Команда `python -m healthcheck` использует тот же Telegram-клиент и журнал `wb_telegram_deliveries`. Новый набор ошибок отправляется один раз, неизменные ошибки не повторяются каждые пять минут. После устранения всех проблем отправляется сообщение о восстановлении.
